var TPLBLOB = TPLBLOB || {}; function TplBlob(name) { return TPLBLOB[name] ? LZString.decompressFromBase64(TPLBLOB[name]) : ""; }function IsTplBlob(name) { return TPLBLOB[name] ? true : false; }console.TplBlob = TplBlob; console.IsTplBlob = IsTplBlob;
function _onDeviceReady() {
    alert("deviceready");
}


    // Bind any events that are required on startup. Common events are:
    // 'load', 'deviceready', 'offline', and 'online'.
document.addEventListener('deviceready', _onDeviceReady, false);

